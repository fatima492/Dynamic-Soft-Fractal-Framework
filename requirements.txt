# Dynamic Soft Fractal Framework — minimal dependencies
# Tested on Python 3.10, 3.11, 3.12
#
# These three packages are sufficient to reproduce every numerical
# result in the manuscript.  No deep-learning, statistical
# (statsmodels, R/lme4), or visualisation framework is required for
# the analysis pipeline itself.

numpy>=1.24
scipy>=1.10
pandas>=2.0
